# mwp-content

Shared content layer for `mwp`.

This crate loads markdown files from the wiki tree, extracts metadata and links, and renders page content that the CLI and older runtime components consume.
