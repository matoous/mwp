# mwp-search

Legacy search primitives for `mwp`.

This crate contains the Tantivy-based indexing structures used by the original server-side search flow before the project moved to static Pagefind output.
