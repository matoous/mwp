# mwp

Command-line interface for the Matt's Wiki Project.

It builds a markdown wiki into a static site, generates a Pagefind index for linked pages, reuses a local fetch cache, and can serve the built output locally for preview.
